#include "main.h"
#include <stdio.h>

/**
 * add - Adds two integers and returns
 * @i: number one.
 * @k: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int i, int k)
{
	return (i + k);
}
